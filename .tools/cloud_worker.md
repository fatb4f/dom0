# cloud_worker

Summary: External bounded execution surface for compute-heavy or long-running tasks that should stay outside the local session, consume localized `dom0` handoff/evidence packets, and return derived artifacts without becoming the authority for raw substrates or root control decisions.
