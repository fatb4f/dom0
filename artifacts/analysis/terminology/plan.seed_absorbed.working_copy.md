# Seed-Absorbed Plan Working Copy

Base plan: `/home/_404/src/dom0/PLAN/README.md`

## Q1

Yes.

The semantic definition of the seeds does reveal collateral surfaces that the current task framing did not make first-class yet. The plan already points at root-level ACM extension, but the seed semantics show that the missing work is broader than `relationship.current`, `identity.current`, and `applied_control.current` alone.

## Collateral Surfaces

- `policy` / `policy_surface`
  The PLAN references plane alignment, but policy is not yet treated as an explicit root-level governed surface.

- `unknown` / `undisclosed`
  The PLAN calls for identity substrate work, but does not yet freeze unknown-state semantics as a required output.

- `service` / `state_space` / `fsm`
  The PLAN names root candidates and scope semantics, but not the full service ontology and FSM-fit definition as explicit D4 outputs.

- `writer` / `subordinate` / `observer`
  Authority is present in the PLAN, but these role distinctions are not yet explicitly absorbed into the root ontology.

- `assurance` / `measure` / `signal`
  Applied control is planned, but the required output semantics for assurance checks, measures, and signals are still implicit.

- `bootstrap_manifest`
  The PLAN assumes a root-level ACM extension path, but the bootstrap binding object family is not yet made explicit.

## Absorbed Seeds

- `policy`
  Absorb into `scope` and Phase 2 as a first-class root-level governed surface aligned to ACM planes.

- `service`
  Absorb into D4 as part of the root ontology.

- `state_space`
  Absorb into `problem_set` and D4 as a required definition output.

- `fsm`
  Absorb into D4 as explicit FSM-fit criteria for `host`, `codex`, and `spawn`.

- `unknown`
  Absorb into D6 as a first-class identity state.

- `undisclosed`
  Absorb into D6 as a live identity mutation path.

- `identity_delta`
  Absorb into D6 and D8 as an explicit re-analysis trigger artifact.

- `writer`
  Absorb into `problem_set` and D4 as a root authority role.

- `subordinate`
  Absorb into `problem_set` and D4 as a root/subsystem role.

- `observer`
  Absorb into `problem_set` and D5 as a relationship-producing role.

- `assurance`
  Absorb into `problem_set` and D7 as required applied-control semantics.

- `measure`
  Absorb into D7 and D8 as intended control outputs/byproducts.

- `signal`
  Absorb into D5 and D7 as relationship/control emission semantics.

- `feedback`
  Absorb into D8 as explicit loop-closure methodology.

- `policy_surface`
  Absorb into `problem_set` and D7 as a collateral control object.

- `bootstrap_manifest`
  Absorb into `problem_set` and Phase 2 as a collateral bootstrap object.

## Working-Copy Delta Against PLAN

- `problem_set`
  Add `state_space`, service ontology, role taxonomy, unknown/undisclosed semantics, and collateral object surfaces (`policy_surface`, `bootstrap_manifest`).

- `scope`
  Add policy-plane definition, service ontology, epistemic-state semantics, and assurance/measurement semantics as in-scope semantic work.

- `D4`
  Add `state_space`, `service`, `fsm`, `writer`, and `subordinate`.

- `D5`
  Add `observer` and relationship `signal` semantics.

- `D6`
  Add `unknown`, `undisclosed`, and `identity_delta`.

- `D7`
  Add `assurance`, `measure`, `signal`, and `policy_surface`.

- `D8`
  Add explicit `feedback` loop closure and `identity_delta` re-analysis triggers.

## Implication

The current PLAN is directionally correct, but seed semantics show it under-specifies several collateral root surfaces. The next revision should not only enrich wording; it should widen the semantic target so those surfaces become explicit deliverable content.
